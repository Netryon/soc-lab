# SOC Lab

Homelab for security operations practice: virtualization, SIEM, log forwarding, and (later) detection experiments.

## Projects (one folder each)

| Folder | What it is |
|--------|------------|
| **[`siem-lab/`](siem-lab/)** | Splunk + Universal Forwarder, Windows logs, milestones |
| *(add more later)* | e.g. `wireshark-lab/`, `active-directory-lab/`, `detection-rules/` |

Keeping **one directory per project** keeps diagrams, writeups, and configs from piling up in the repo root.

## Repo root

Only **`README.md`** (this file) and optional top-level notes (e.g. `CONTRIBUTING.md`) should live here.

## Related

- TryHackMe / investigation writeups: [`security-writeups`](../security-writeups) (sibling repo).
- Personal backup / infra project: [`chronovault`](../chronovault).
